version: 0.2

phases:
      
  pre_build:
    commands:
      -  sudo apt-get install zip
      -  sudo apt-get install unzip
      -  sudo apt-get install libncurses5
      
  build:
    commands:
      - bash prova.sh
      #- lunch rpi4-eng
      #- make ramdisk systemimage vendorimage -jx

  post_build:
    commands:
      - echo "Sono in Post Build"
